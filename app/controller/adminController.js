const { Alimentaire, Bébé, Entretien, Hygiène, Liste } = require('../models');

module.exports = {
    async handlealiment(request, response) {
        // on récupére le nouveau nom (transmis par le formulaire)
        // dans le corps de la requête
        
      },
}